---
category: minorAnalysis
---
* The `actions/unpinned-tag` query now reports third-party reusable workflows referenced by mutable tags or branches. The `actions/excessive-secrets-exposure` query now reports reusable workflow calls that use `secrets: inherit`.
