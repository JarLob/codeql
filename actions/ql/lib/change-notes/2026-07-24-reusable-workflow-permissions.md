---
category: feature
---
* Added `Permissions.getConfiguredPermission` and `Job.getEffectivePermission` to model normalized `GITHUB_TOKEN` permissions and caller-imposed permission caps across nested reusable workflows. Permission shorthands and current permission scopes are supported.
