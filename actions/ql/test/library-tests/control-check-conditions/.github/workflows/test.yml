on:
  pull_request_target:

jobs:
  exact-member:
    if: github.event.pull_request.author_association == 'MEMBER'
    runs-on: ubuntu-latest
    steps:
      - run: echo exact member

  reversed-owner:
    if: "'OWNER' == github.event.pull_request.author_association"
    runs-on: ubuntu-latest
    steps:
      - run: echo reversed owner

  bypassable-member:
    if: github.event.pull_request.author_association == 'MEMBER' || vars.force == true
    runs-on: ubuntu-latest
    steps:
      - run: echo bypassable member

  label-only:
    if: contains(github.event.pull_request.labels.*.name, 'safe-to-test')
    runs-on: ubuntu-latest
    steps:
      - run: echo label

  non-fork-exact:
    if: github.event.pull_request.head.repo.fork == false
    runs-on: ubuntu-latest
    steps:
      - run: echo local branch

  non-fork-reversed:
    if: false == github.event.pull_request.head.repo.fork
    runs-on: ubuntu-latest
    steps:
      - run: echo local branch

  non-fork-not-true:
    if: github.event.pull_request.head.repo.fork != true
    runs-on: ubuntu-latest
    steps:
      - run: echo local branch

  non-fork-negated:
    if: "!github.event.pull_request.head.repo.fork"
    runs-on: ubuntu-latest
    steps:
      - run: echo local branch

  non-fork-bypassable:
    if: github.event.pull_request.head.repo.fork == false || vars.force
    runs-on: ubuntu-latest
    steps:
      - run: echo bypassable

  fork-exact:
    if: github.event.pull_request.head.repo.fork == true
    runs-on: ubuntu-latest
    steps:
      - run: echo fork

  fork-not-false:
    if: github.event.pull_request.head.repo.fork != false
    runs-on: ubuntu-latest
    steps:
      - run: echo fork

  non-fork-string-false:
    if: github.event.pull_request.head.repo.fork == 'false'
    runs-on: ubuntu-latest
    steps:
      - run: echo string false

  non-fork-string-not-true:
    if: github.event.pull_request.head.repo.fork != 'true'
    runs-on: ubuntu-latest
    steps:
      - run: echo string true

  identity-or-non-fork:
    if: >-
      github.event.pull_request.author_association == 'MEMBER' ||
      github.event.pull_request.head.repo.fork == false
    runs-on: ubuntu-latest
    steps:
      - run: echo trusted identity or local branch

  renovate-non-fork:
    if: >-
      (github.actor == 'renovate[bot]' ||
      contains(github.event.pull_request.labels.*.name, 'renovatebot')) &&
      github.event.pull_request.head.repo.fork == false
    runs-on: ubuntu-latest
    steps:
      - run: echo renovate