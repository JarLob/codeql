name: Issues downstream # Expected: externally triggerable through the issues source.
on:
  workflow_run:
    workflows: [Issues source]
    branches: [main]
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
