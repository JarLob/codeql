name: Branch ordered reinclude downstream # Expected: final pattern re-includes main.
on:
  workflow_run:
    workflows: [Issues source]
    branches: ['*', '!main', 'main']
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream