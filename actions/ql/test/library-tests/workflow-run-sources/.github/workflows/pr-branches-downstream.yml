name: PR branches downstream # Expected: positive branches filter blocks fork PR runs.
on:
  workflow_run:
    workflows: [PR source]
    branches: ['**']
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
