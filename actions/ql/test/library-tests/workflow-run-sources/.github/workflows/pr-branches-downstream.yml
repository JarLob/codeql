name: PR branches downstream # Expected: reviews remain external; direct fork PR runs are blocked.
on:
  workflow_run:
    workflows: [PR source]
    branches: ['**']
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
