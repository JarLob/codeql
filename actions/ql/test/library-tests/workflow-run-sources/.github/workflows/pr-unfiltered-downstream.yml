name: PR unfiltered downstream # Expected: externally triggerable by a fork PR run.
on:
  workflow_run:
    workflows: [PR source]
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
