name: PR source # Expected: review triggers can use externally influenced base-repository runs.
on:
  pull_request:
  pull_request_review:
  pull_request_review_comment:
jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - run: echo source
