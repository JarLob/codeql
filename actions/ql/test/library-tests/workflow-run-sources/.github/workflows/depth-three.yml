name: Depth three # Expected: third workflow_run level is accepted.
on:
  workflow_run:
    workflows: [Depth two]
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo three