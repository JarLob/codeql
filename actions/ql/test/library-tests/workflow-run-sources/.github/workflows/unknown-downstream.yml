name: Unknown downstream # Expected: unresolved source remains conservatively external.
on:
  workflow_run:
    workflows: [Missing source]
    branches: [main]
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
