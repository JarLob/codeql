name: Unknown downstream # Expected: valid filter with no local match cannot trigger.
on:
  workflow_run:
    workflows: [Missing source]
    branches: [main]
    types: [completed]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo downstream
