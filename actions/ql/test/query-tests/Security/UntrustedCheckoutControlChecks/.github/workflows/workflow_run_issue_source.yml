name: Comment source # Expected: comment and review runs exercise source-specific controls.
on:
  issue_comment:
  pull_request_review:
  push:
    branches: [main]
jobs:
  source:
    runs-on: ubuntu-latest
    steps:
      - run: echo source
