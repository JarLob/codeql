name: Composite checkout by ref

inputs:
  ref:
    description: Git reference to check out
    required: true

runs:
  using: composite
  steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ inputs.ref }}
    - shell: bash
      run: pnpm i