name: Runner PR unfiltered consumer # Expected: self-hosted runner alert.
on:
  workflow_run:
    workflows: [Runner PR source]
    types: [completed]
jobs:
  pr-unfiltered:
    runs-on: self-hosted
    steps:
      - run: echo pr
