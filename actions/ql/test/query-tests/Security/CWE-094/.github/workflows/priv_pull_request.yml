name: Privileged (only when local) pull request

on:
  pull_request:

permissions:
  pull-requests: write
  contents: write

jobs:
  test: # Expected: low alert; pull_request isolates the injected code from untrusted forks.
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.pull_request.body }}"
