name: Fork PR positive branches # Expected: no alert; positive branches reject fork PR runs.

on:
  workflow_run:
    workflows: ["Pull-Request Checks"]
    branches: ["feat/**"]
    types: [completed]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.workflow_run.display_title }}
