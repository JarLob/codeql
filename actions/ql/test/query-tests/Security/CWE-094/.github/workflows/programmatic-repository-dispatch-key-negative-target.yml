name: Programmatic repository dispatch key negative target

on:
  repository_dispatch:
    types: [key-negative]

permissions:
  contents: write

jobs:
  consume:
    runs-on: ubuntu-latest
    steps:
      # Not expected: attacker-controlled data is dispatched only to `client_payload.safe`.
      - run: echo '${{ github.event.client_payload.message }}'