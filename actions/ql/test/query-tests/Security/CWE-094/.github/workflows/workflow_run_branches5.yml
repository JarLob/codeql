name: Fork PR ignored branches # Expected: code injection alert; negative filters allow fork PR runs.

on:
  workflow_run:
    workflows: ["Pull-Request Checks"]
    branches-ignore: ["foo"]
    types: [completed]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.workflow_run.head_branch }}
