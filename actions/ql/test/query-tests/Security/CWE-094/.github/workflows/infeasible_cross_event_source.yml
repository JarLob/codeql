on:
  pull_request_target:
  workflow_dispatch:

jobs:
  test:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      # Expected: no alert; the PR body is unavailable in the only context that runs this step.
      - run: echo '${{ github.event.pull_request.body }}'