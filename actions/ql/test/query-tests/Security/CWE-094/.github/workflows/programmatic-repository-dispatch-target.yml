name: Programmatic repository dispatch target

on:
  repository_dispatch:
    types: [security-scan]

permissions:
  contents: write

jobs:
  consume:
    runs-on: ubuntu-latest
    steps:
      # Expected: actions/code-injection/critical
      - run: echo '${{ github.event.client_payload.message }}'