on:
  pull_request_target:

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/dangerous-git-checkout
      - run: npm install

  guarded:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/guarded-git-checkout
      - run: npm install